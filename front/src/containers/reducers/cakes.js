
export const initialState = {
  list: [],
}

const reducer = (currentState = initialState, action= {}) => {
  switch (action.type) {
    default:
      return currentState;
  }
}

export default reducer;
